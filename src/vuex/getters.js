// app
export const loaded = state => state.app.loaded

// config
export const config = state => state.app.config

// paths
export const newPath = state => state.paths.newPath
export const pathList = state => state.paths.list
export const currentPath = state => state.paths.currentPath

// people
export const peopleList = state => state.people.list
